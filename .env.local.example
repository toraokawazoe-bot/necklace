NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# --- Instagram DM 連携（後から設定可。未設定でもアプリは通常どおり動作）---
# Webhook の合言葉。自分で決めた任意の文字列を入れ、Meta の Webhook 設定にも同じ値を入力する
INSTAGRAM_VERIFY_TOKEN=
# Meta アプリの App Secret。Webhook 署名の検証に使う（未設定だと検証をスキップ＝開発用のみ）
INSTAGRAM_APP_SECRET=
# 長期アクセストークン。用途は2つ:
#  (1) DM 送信者のユーザー名を自動取得してカードに表示（任意）
#  (2) 管理画面からの DM 返信送信（返信機能を使うなら必須）
# 返信送信には instagram_business_manage_messages 権限と、長期（60日）Instagram User トークンが必要。
# 権限が無い／期限切れの場合、返信は権限エラーや190エラーで失敗する。
INSTAGRAM_ACCESS_TOKEN=

# --- セキュリティ（運用者ログイン）。手順は SECURITY_SETUP.md 参照 ---
# サーバー専用の Firebase サービスアカウント鍵（JSON文字列 or base64）。
# webhook / 送信 API が Firestore ルールをバイパスして書き込むために必須。
# Firebase Console → プロジェクト設定 → サービスアカウント → 新しい秘密鍵を生成 で取得。
FIREBASE_SERVICE_ACCOUNT=
# 運用者の Firebase UID（Authentication → Users）。送信 API を本人限定にする（推奨）。
# 設定すると、認証済みでも UID 不一致は 403 で弾く。未設定なら「認証済みなら誰でも」。
OWNER_UID=
# "1" にするとブラウザでログイン必須になり、送信 API も本人確認する。
# 設定一式（Auth有効化・運用者アカウント作成・上の鍵設定・ルール厳格化）が
# 済んでから "1" にすること（途中で立てると締め出される）。未設定なら従来どおり動作。
NEXT_PUBLIC_REQUIRE_AUTH=
